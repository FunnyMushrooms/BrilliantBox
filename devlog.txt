# Dev Log

## Iteration 1
- Built initial static GitHub Pages-compatible project scaffold.
- Implemented core engine, scoring, storage, UI, scenario loader, data files, validator, and optional remote worker.
- Added README and develop.txt documentation.
- Note: `dental.png` was not found in repository at implementation time, so no direct flow cross-check could be performed.

## Iteration 2
- Refactored scenario content to avoid one oversized string-heavy file.
- Introduced fragment-based scenario loading (`data/scenario.json` manifest + per-network files in `data/scenario_fragments/`).
- Updated validator to validate merged fragments and preserved core checks (references, reachability, positive time costs, win path).
- Updated docs (`README.md`, `develop.txt`) to explain modular scenario architecture and extension flow.
- Re-checked for `dental.png`; still not present in repo, so comparison with that visual flow remains pending.
- Discussion note: if `dental.png` is later provided, we should map its Telegram/web action flow against current node hubs and identify missing transitions.

## Iteration 3
- Reworked UI to a more visual mission-board style with richer gradients, card-like actions, machine context panel, and host badges with platform icons.
- Replaced plain-text network map tiles with a subnet SVG map that shows SOC center, linked segments, and lock/unlock status icons.
- Added machine-aware investigation hints and tool suggestions per platform (Windows/Linux/OT/appliance), including interactive command previews on option hover/focus (e.g., nmap variants).
- Extended `data/hostPool.json` with `platform` and `role` metadata so gameplay can surface OS-specific checks such as Task Scheduler/Registry/SMB for Windows and cron/services/SSH for Linux.
- Checked provided Dental flow image from the prompt context (telegram/web action inspiration) and aligned current iteration toward visual mission cards and explicit action affordances, while keeping existing scenario graph flow.

## Iteration 4
- Addressed feedback about engagement: added dark-humor incident prompts and machine-specific “interesting question” cards (registry oddities, suspicious host load, OT anomalies).
- Reduced visible action overload by capping options per turn (2 in Casual Fun, 3 in Analyst Mode) while preserving underlying scenario flow.
- Added difficulty selector with two modes:
  - Casual Fun: guided/plain-language hints and everyday-style missions (USB lost, Instagram blocked, noisy laptop).
  - Analyst Mode: command-oriented triage flow similar to prior behavior.
- Implemented malware visibility + interaction in sidebar:
  - Newly found infected hosts appear as malware cards with status and threat type.
  - Added action buttons (Analyze, Quarantine, Eradicate) with time/score/noise consequences.
- Extended engine state/reporting with malware sightings and interaction handling.
- Re-checked Dental image intent from prompt context and kept alignment on clear visual actions + card interactions in web flow; telegram-specific flow still to be mapped if a formal in-repo asset/spec is provided.

## Iteration 5
- Implemented a full four-network mission map with direct machine-click navigation from the visual SVG map (Domain/Public, Corporate Domain, ICS/Power Grid, Military UAV).
- Reauthored scenario fragments around machine-specific roles/tasks so each machine has unique purpose and investigation flow.
- Added explicit routing-service collapse outcomes in provider/corp/ot/drone machine decisions when firewall routing is blocked.
- Updated host catalog to match scenario machines with unique `role` + `description` metadata (including HR workstation constraints and file-focused investigations).
- Changed timer behavior to run independently in real time via a global tick; player actions and malware interactions no longer consume timer budget.
- Added cyber kill-chain sidebar panel with hidden stages that unlock through IOC/malware discovery and malware-card actions (analyze/eradicate).
- Expanded narrative outcomes for ransomware file-deletion prevention, compromised users, malicious config changes, UAV-in-flight disruption, ICS power/light disruption, and unavailable public sources.
- Checked prompt-provided Dental image context and kept alignment around clickable web interaction flow; exact Telegram-specific pathing remains open for follow-up mapping discussion if a formal in-repo spec is added.

## Iteration 6
- Expanded map usability by enlarging the SVG topology, machine hit targets, and labels to improve clickability for fast machine pivots.
- Added an interactive firewall panel showing connected inter-network channels (e.g., SMB historian sync, UAV stream relay) with allow/block controls and gameplay consequences.
- Implemented process analysis lab per machine:
  - Shows platform-specific process list with suspicious/critical verdicts.
  - Added Analyze flow with 1.4s popup delay and function-level result details.
  - Added Block action with cascading consequences (e.g., blocking `uav.exe` can drop UAV network access).
- Added continuous outage penalties after 2 minutes when networks/channels remain disrupted, matching scenario pressure for unavailable public resources.
- Expanded Analyst-mode command flavor for Windows/Linux/SCADA-style triage (`Get-Process`, `/etc/passwd`, modbus read samples).
- Updated scenario narrative to emphasize one connected campaign (phishing, ransomware, lateral movement, reverse channels, backdoors) and kill-chain dependency mapping.
- Re-checked repository for `dental.png`: still not present. Kept implementation aligned with requested web interactions and noted direct dental flow parity still requires the actual image/spec file.

## Iteration 7
- Reworked the network map into a more visual topology-first interaction: clickable machine icon tiles + clickable firewall knobs on links + clickable SOC core for final kill-chain review.
- Removed dependence on text-only hub actions by guiding users to use the visual map for machine/network pivots.
- Added transition popups (1-2 sec) for network/machine/firewall operations to simulate real analyst action latency.
- Updated process analysis timing model:
  - Benign/simple process analysis now ~2s.
  - Suspicious/malicious executables now take 5-10s before verdict output.
- Added richer “real-life” analyst command style across Windows/Linux/ICS playbooks (e.g., Get-Process, /etc/passwd review, Modbus read-only checks).
- Added remediation guidance panel (“What to Rectify”) to explain what is working, what is broken, and what must be fixed next.
- Added interactive “Live Service Windows” (public portal, banking, HR, ICS panel, UAV stream) as popup previews to investigate app symptoms and dependency hints.
- Re-checked for `dental.png`: still not present in repository; implementation continued following devlog-first requirement and web interaction direction from user notes.
